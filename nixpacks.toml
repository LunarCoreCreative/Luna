# Nixpacks config for Railway
# Use Python provider, let Node be detected automatically

providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m server.main"
